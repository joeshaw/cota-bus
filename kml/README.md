Download the COTA static GTFS file from https://www.cota.com/data/

Run `go run ../tools/route-kml.go ../OpenGTFSData.zip`.  KML files will be
output in the current directory.  Filenames are `<route_id>.kml`.
